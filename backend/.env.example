# ========================================
# CONFIGURACIÓN DE VARIABLES DE ENTORNO
# ========================================
#
# Este archivo es un ejemplo. Copia este archivo como .env y ajusta los valores.
# Comando: cp .env.example .env
#
# IMPORTANTE: Nunca subas el archivo .env al repositorio (está en .gitignore)

# ========================================
# CONFIGURACIÓN PARA DESARROLLO LOCAL
# ========================================
# Usa estos valores cuando ejecutes: python3 app.py

DB_HOST=localhost
DB_USER=pythonUser
DB_PASSWORD=python1234
DB_NAME=VISIT_REGISTRY_DB
PORT=5000

# ========================================
# CONFIGURACIÓN PARA DOCKER
# ========================================
# Cuando se use Docker (docker-compose up), las variables se configuran
# automáticamente en docker-compose.yml. NO necesitas crear un archivo .env
# para Docker, pero si lo haces, usa estos valores:
#
# DB_HOST=mysql
# DB_USER=pythonUser
# DB_PASSWORD=python1234
# DB_NAME=VISIT_REGISTRY_DB
# PORT=5000

# ========================================
# NOTAS
# ========================================
# - DB_HOST debe ser 'localhost' para desarrollo local
# - DB_HOST debe ser 'mysql' para Docker (nombre del servicio)
# - Se pueden cambiar las credenciales según tu configuración de MySQL local
